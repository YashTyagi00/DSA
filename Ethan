#include <iostream>
#include <stack>
using namespace std;

int main() {
    int n;
    cin >> n; 
    stack<int> myStack;

    for (int i = 0; i < n; i++) {
        int element;
        cin >> element;
        myStack.push(element);
    }

    int searchElement;
    cin >> searchElement; 

    bool found = false;
    
    while (!myStack.empty()) {
        int topElement = myStack.top();
        myStack.pop();
        if (topElement == searchElement) {
            found = true;
            break; 
        }
    }

    if (found) {
        cout << "Element found" << endl;
    } else {
        cout << "Element not found" << endl;
    }

    return 0;
}

